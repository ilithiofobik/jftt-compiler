VAR
	a, b
BEGIN
	READ a;
	a ASSIGN 5 PLUS a;
	b ASSIGN a MINUS 2047;
	b ASSIGN b DIV 1;
    a ASSIGN a TIMES 1;
	IF a LE b THEN
		WRITE 111111;
	ELSE
		WRITE a;
	ENDIF
	IF b LEQ a THEN
		WRITE b;
	ELSE
		WRITE 111111;
	ENDIF
END
